class Solution:
    def largestElement(self, nums):
        length = len(nums)
        largest = nums[0]
        for i in range (1,length):
            if largest <= nums[i] :
                largest = nums[i]
        return largest
